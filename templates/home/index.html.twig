{% extends 'base.html.twig' %}

{% block title %}Accueil{% endblock %}

{% block body %}
<div class="container mx-auto px-4 py-8">
    <h1 class="text-4xl font-bold text-center mb-8">Bienvenue dans l'application de gestion</h1>
    
    <div class="flex justify-center space-x-8">
        <a href="{{ path('app_category_index') }}" class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-4 px-8 rounded-lg text-xl transition duration-300 ease-in-out transform hover:scale-105">
            Gérer les Catégories
        </a>
        
        <a href="{{ path('app_reminder_index') }}" class="bg-green-500 hover:bg-green-700 text-white font-bold py-4 px-8 rounded-lg text-xl transition duration-300 ease-in-out transform hover:scale-105">
            Gérer les Rappels
        </a>
    </div>
</div>
{% endblock %}